<!DOCTYPE html>
<html>
<head>
  <title>何切るドットコム</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= turbo_include_tags %>
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= favicon_link_tag 'favicon.ico' %>

  <!-- ▼▼▼ ここからOGPタグ ▼▼▼ -->
  <meta property="og:title" content="<%= content_for?(:og_title) ? yield(:og_title) : '何切るドットコム' %>" />
  <meta property="og:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '麻雀の何切る問題をみんなで考える掲示板サイト' %>" />
  <meta property="og:image" content="<%= content_for?(:og_image) ? yield(:og_image) : image_url('ogp_default.png') %>" />
  <meta property="og:url" content="<%= request.original_url %>" />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="何切るドットコム" />
  <meta name="twitter:card" content="summary_large_image" />
  <!-- 任意で↓も追加（公式Twitterがあれば） -->
  <!-- <meta name="twitter:site" content="@your_twitter_account" /> -->
  <!-- ▲▲▲ ここまでOGPタグ ▲▲▲ -->
</head>
<body>
  <%= render 'layouts/header' %>

  <main class="site-main">
    <div class="container">
      <% flash.each do |key, message| %>
        <div class="flash <%= key %>"><%= message %></div>
      <% end %>

      <%= yield %>
    </div>
  </main>

  <%= render 'layouts/footer' %>
</body>
</html>
